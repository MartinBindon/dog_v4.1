<?xml version="1.0" encoding="UTF-8" ?>
    <widget xmlns   = "http://www.w3.org/ns/widgets"
        xmlns:gap   = "http://phonegap.com/ns/1.0"
        id          = "com.phonegap.example"
        versionCode = "10" 
        version     = "1.0.0" >

    <!-- versionCode is optional and Android only -->

    <name>dogApp_build3</name>

    <description>
        The beginnings of my dog application. Trying to get Google Maps and Geolocation working. 
    </description>

    <author href="" email="">
        Martin Bindon 
    </author>

    <gap:platform name="android" />

    <!-- remove this as it is out of date and from old documentation -->
    <!-- <plugin name="Geolocation" value="org.apache.cordova.geolocation" /> -->

    <gap:plugin name="org.apache.cordova.geolocation" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />

    <access origin="*://*.googleapis.com/*" subdomains="true" />
    <access origin="*://*.gstatic.com/*" subdomains="true" />
    <access origin="*://*.google.com/*" subdomains="true" />
    <access origin="*://*.googleusercontent.com/*" subdomains="true" />
</widget>


